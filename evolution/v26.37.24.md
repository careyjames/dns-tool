# DNS Tool Evolution — v26.37.24

## Date: March 17, 2026

## Summary
Web3 domain analysis: complete pipeline from ENS/HNS input resolution through IPFS fleet probing with authority containment.

## Changes

### Web3 Phase 2 — ENS/HNS Input Resolution Engine
- `.eth` domain resolution via `eth.limo` gateway
- Handshake TLD resolution via `hnsdns.com` / `hdns.io` public resolvers
- Web3 inputs detected before `checkDomainExists()` in orchestrator
- Resolution info displayed in dedicated Web3 card in results.html

### Web3 Phase 3–5 — Typed Evidence Model, Input Classification, Attribution Guard
- `InputKind` classification: `ens_name`, `hns_name`, `dns_domain`
- `AnalysisScope` prevents false attribution of gateway email configs to ENS owners
- Typed evidence: `IPFSProbe` struct (namespace/cid/gateways_tested/trust_mode/persistence_unproven)
- `Web3DNSSECTrust` struct (structured bools for DNSSEC trust evaluation)
- `evidence_class` on indicators: `protocol_binding` vs `identity_metadata`
- `_dnslink.<host>` queried first (canonical), root TXT as fallback, `dnslink_source` tracking
- Attribution warnings for gateway-derived domains

### Web3 Phase 6 — Authority Containment (S001–S007)
- Scope constants: `ScopeOwnedDNS` ("owned_dns"), `ScopeGatewayDerived` ("gateway_derived"), `ScopeIdentityOnly` ("identity_only")
- `buildGatewayPosture()` returns attribution-limited stub (score=0, grade="N/A")
- All badge paths (flat/covert/detailed/ShieldsIO) apply gateway override via `isGatewayDerivedResult()`
- HNS via public resolvers gets `ScopeGatewayDerived` + attribution warning
- `_analysis_provenance` map and `_schema_version=2` injected into all result paths
- 13 unit tests for S001–S007 validation matrix

### Web3 Phase 7 — IPFS Fleet Probe (W001–W005)
- **W001**: Probe agent `/probe/ipfs` endpoint with CID validation, gateway allowlist, bounded parallelism (3 concurrent), redirect chain capture, SSRF protection
- **W002**: Server-side fleet client (`web3_probe.go`) with parallel probe dispatch, consensus logic, infrastructure fingerprinting
- **W003**: Orchestrator integration (`enrichWeb3WithFleetProbe`) upgrades `trust_mode` from `gateway_trusted` to `fleet_verified`
- **W004**: 22 unit tests — parallel dispatch, probe failure tolerance, auth header forwarding, hostile CID rejection, redirect divergence, consensus thresholds, persistence upgrade
- **W005**: Template rendering — per-probe reachability matrix, persistence verdict, infrastructure fingerprint in results.html

### Security Hardening
- SSRF redirect containment: `isAllowlistedGatewayHost()` validates redirect targets against allowlisted IPFS gateways (exact + subdomain match)
- `X-Probe-Key` authentication header forwarding verified by test

## Key Files
- `go-server/cmd/probe/main.go` — Probe agent IPFS endpoint
- `go-server/internal/analyzer/web3_probe.go` — Fleet client + consensus
- `go-server/internal/analyzer/web3_probe_test.go` — 22 tests
- `go-server/internal/analyzer/web3.go` — Web3 analysis core
- `go-server/internal/analyzer/orchestrator.go` — Fleet probe integration
- `go-server/internal/config/config.go` — IPFS_PROBE_MODE config
- `go-server/templates/results.html` — Fleet probe UI rendering

## Quality Gates
- Go tests: 22 IPFS fleet probe tests PASS
- Phase 6 validation matrix: 13/13 PASS (5 code paths, 4 badge paths, 3 schema paths)
- Server binary: compiles and starts on :5000
- SSRF: redirect containment verified

## ACIP Verification
- **What changed?** Web3 domain analysis pipeline (phases 2–7): input resolution, evidence typing, authority containment, IPFS fleet probing
- **What could break?** Badge output for ENS/HNS domains (gateway override), IPFS probe results (remote mode only)
- **How to verify?** Run `go test -run "TestIPFS|TestWeb3|TestEnrich" ./go-server/internal/analyzer/`; scan ENS domain; check badge for gateway-derived scope; verify probe matrix in results.html
